#include <stdio.h>
#include "record_struct.h"

#ifndef SORT_REC_H
#define SORT_REC_H

/* 
 * sortRec()
 * ---------
 * Sorts the records by their name.
 * 
 * data : the variable to store the records read from text file
 */
void sortRec(struct record* data);

#endif
